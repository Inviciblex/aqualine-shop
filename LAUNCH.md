# Чек-лист к запуску

Пройдите перед тем, как открывать сайт публично. Все плейсхолдеры — в квадратных
скобках `[...]` или с пометкой «ЗАМЕНИТЕ» в коде; найти разом:

```bash
grep -rn "ваш-домен\.ру\|\[.*\]\|ЗАМЕНИТЕ\|example\.ru\|@aqualine\|+7 (000)" \
  index.html public/ src/ | grep -v node_modules
```

---

## 1. Контакты и реквизиты

- [x] **`src/store.js`** — телефон, Telegram, email, адрес, часы заполнены:
  - телефон `+7 (918) 219-19-54`, Telegram `@fanto2000`, email `stoevandrey@yandex.ru`
  - адрес: Краснодар, Володи Головатого, 286/1
  - часы: ежедневно 8:00–17:00, без выходных
- [ ] **`src/components/Contacts.jsx`** — реквизиты `[ИП/ООО «Название», ИНН …]`
- [ ] **`src/components/PrivacyPolicy.jsx`** — `[НАЗВАНИЕ … ИНН]` и дата `[ДД.ММ.ГГГГ]` (email/телефон/адрес уже подставлены)
- [ ] **`src/components/Warranty.jsx`** — дата `[ДД.ММ.ГГГГ]` (почта подставлена)
- [ ] **`src/components/Returns.jsx`** — дата `[ДД.ММ.ГГГГ]` (почта подставлена)

## 2. Домен и SEO

- [ ] **`index.html`** — `canonical`: заменить `https://ваш-домен.ру/`
- [ ] **`index.html`** — заменить домен в `og:url`/`og:image`/`twitter:image` (теги раскомментированы; на странице товара их подставляет `src/seo.js`, домен нужен для главной)
- [ ] **`index.html`** — Store JSON-LD: `telephone`/`openingHours` заполнены; осталось вписать `url` (после домена)
- [ ] **`public/sitemap.xml`** — заменить `ваш-домен.ру` во всех `<loc>` (товарные `/product/N` дописываются на сборке и берут домен из первого `<loc>` автоматически)
- [ ] **`public/robots.txt`** — заменить `ваш-домен.ру` (строка 7, `Sitemap:`)

## 3. Карта и превью

- [x] **`src/store.js`** — `MAPS_URL`/`MAPS_EMBED_URL`: точная точка магазина (share-ссылка `CTQ3bHNb`).
- [ ] **`public/og-image.png`** — добавить картинку 1200×630 для превью при шеринге

## 4. Бэкенд: `server/.env` (секреты, НЕ в git)

Скопируйте `server/.env.example` → `server/.env` и заполните:

- [ ] `TG_BOT_TOKEN` — токен бота от @BotFather
- [ ] `TG_CHAT_ID` — ID чата для заказов
- [ ] `ADMIN_TOKEN` — длинная случайная строка (вход в админку `/admin`); пусто = админка выключена
- [ ] `ALLOWED_ORIGIN` — ваш домен (например `https://shop.example.ru`), не `*`
- [ ] Проверить, что бот реально шлёт сообщение (оформить тестовую бронь)

## 5. Инфраструктура

- [ ] **Сервер (VPS)** + **домен** + **DNS** на его IP
- [ ] **HTTPS** — поставить Caddy перед `web` (авто-сертификаты Let's Encrypt).
      Делается, когда есть домен и сервер (см. разговор/README)
- [ ] **`VITE_ORDER_API_URL`** — при сборке web должен быть задан (в `docker-compose*.yml`
      уже `/api/order`); проверить, что НЕ ушли в демо-режим (иначе заказы не доходят до сервера)
- [ ] **`.env` для compose** (`.env.deploy.example` → `.env`): `IMAGE_PREFIX`, `TAG`
- [ ] **Фаервол** — открыть только 80/443; `api` наружу не публиковать (уже `expose`)

## 6. Бэкапы

- [ ] Настроить cron: `deploy/backup.sh` (см. README, раздел «Бэкапы базы заказов»)
- [ ] **Увозить копии offsite** (другой диск/облако) — иначе при потере сервера пропадут и база, и бэкапы

## 7. Контент

- [ ] **Реальные фото товаров** вместо заглушек (`images` в Google-таблице или `public/products.json`)
- [ ] Проверить каталог: цены, наличие, бренды, страна и гарантия в характеристиках

## 8. Проверка после запуска (smoke-тест)

- [ ] Сайт открывается по HTTPS, нет ошибок в консоли
- [ ] Оформление брони → пришло уведомление в Telegram, заказ виден в админке
- [ ] Статус заказа меняется в админке и подтягивается в «Мои брони»
- [ ] Кнопки «Позвонить»/«Telegram» и карта на «Контактах» ведут на реальные данные
- [ ] Ссылка с фильтрами (`#/?q=…`) открывается с тем же набором фильтров
